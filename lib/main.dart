import 'package:flutter/material.dart';

void main() =>
    runApp(Center(child: Text('data', textDirection: TextDirection.ltr)));

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
